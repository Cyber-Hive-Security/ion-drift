services:
  ion-drift:
    build: .
    container_name: ion-drift
    hostname: hiverouter
    restart: unless-stopped
    cap_add:
      - NET_RAW
      - NET_ADMIN
    ports:
      - "3100:3000"
      - "5514:5514/udp"
    environment:
      - RUST_LOG=info,tower_http=warn,hyper=warn,mikrotik_core::snmp_client=debug,ion_drift_web::topology_inference=debug,ion_drift_web::correlation_engine=debug
      - XDG_DATA_HOME=/app/data
      - TOPOLOGY_INFERENCE_MODE=active
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    volumes:
      - ion-drift-data:/app/data

volumes:
  ion-drift-data:
