// VLAN configuration is now loaded from the API via useVlanLookup() hook.
// This file is kept for type re-exports only.
// Use `import { useVlanLookup } from "@/hooks/use-vlan-lookup"` in components.

export type { VlanConfig } from "@/api/types";
