# Build artifacts
/target/

# Environment and secrets
.env
.env.*
!.env.example

# Local config (contains machine-specific paths)
config/server.toml

# Certificates (user-provided, local dev)
certs/
*.pem
*.key

# Allow Smallstep root CA in docker/ (public cert, needed for builds)
!docker/root_ca.crt

# Node modules (frontend)
web/node_modules/
# web/dist/ — tracked so git pull deploys built assets

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Spec docs and diagnostics (live on development branch)
SpecDocs/
diagnostics/

# Claude
CLAUDE.md
.claude/

# Codeforge
.codeforge/
